#include <bits/stdc++.h>
using namespace std;
#define fio                           \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)
#define ll long long
const int N = 1e5 + 1;
vector<int> v(N, true);
void sieve()
{
    for (ll i = 2; i < N; ++i)
    {
        v[i].push_back(i);
        for (ll)
    }
}
int main()
{
    fio;
    int t;
    cin >> t;
    while (t--)
    {
    }
    return 0;
}
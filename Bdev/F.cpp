#include<bits/stdc++.h>
using namespace std;
#define fio ios_base::sync_with_stdio(false); cin.tie(NULL)

int main() {
    fio;
    int t;
    cin >> t;
    while (t--) {
        long long n,s,sum=0;
        vector<long long>v;
        cin>>n>>s;
        for(int i=1;i<=n;++i)
        {
            if(sum!=s){
            sum+=i;
            v.push
            }
        }       
    }

    return 0;
}